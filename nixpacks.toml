[phases.setup]
# better-sqlite3 のネイティブコンパイルに必要なツールを追加
nixPkgs = ["nodejs_22", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server.js"
